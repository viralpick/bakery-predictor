# 4매장 — 매장간 비교가 필요한 항목 위주(광교 예측 보조 데이터 관점)
# fix(final review 2): closing_discount/other_discounts/popularity_stockout는 광교
# 전용 discount_rows(store_code 스코프)를 4매장 daily/waste와 섞어 쓰기 때문에
# needs_single_store 게이트가 걸린다(스킵됨) — 확인 결과 이 파일엔 원래 켜져 있지
# 않았으므로 제거할 항목은 없다.
name: analysis_multistore
data:
  source: real
  store: multistore
alpha: 0.8
data_analyses:
  sales_distribution: true
  category_mix: true
  waste_rate: true
hypotheses:
  demand_absorption: true
  stockout_lost_demand: true
